# 임베딩 파인튜닝 환경. 런타임에는 필요 없다.
#
#   pip install -r requirements.txt -r requirements-finetune.txt
#
# 대상 모델은 intfloat/multilingual-e5-small (118M 파라미터, 384차원).
# Apple Silicon 에서는 MPS 가속을 쓴다 — CUDA 는 없다.
#
# ⚠ spec 2.3 은 임베딩 파인튜닝을 8주 범위에서 제외했다(학습 데이터 구축·평가에
#   2주 이상 추가). 환경은 갖췄지만 실제 학습 착수는 일정 재조정이 필요하다.

-r requirements.txt

torch>=2.6
sentence-transformers>=3.3
transformers>=4.48
datasets>=3.2
accelerate>=1.3
peft>=0.14
scikit-learn>=1.6
