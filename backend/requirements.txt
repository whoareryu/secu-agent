# pyproject.toml 이 단일 출처다. 이 파일은 아래 명령으로 재생성한다:
#   uv pip compile pyproject.toml -o requirements.txt

fastapi>=0.115
langchain>=1.3
langchain-google-genai>=4.3
langgraph>=1.2
psycopg[binary]>=3.2
pypdf>=5.1
pyyaml>=6.0
sentence-transformers>=3.3
uvicorn[standard]>=0.32
