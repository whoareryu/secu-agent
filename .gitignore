# Jekyll
jekyll/_site/
jekyll/.jekyll-cache/
jekyll/.jekyll-metadata
jekyll/vendor/
.bundle/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Node
node_modules/
.next/

# 환경 변수 · 자격증명
.env
.env.*
!.env.example

# 데이터 · 모델 (대용량)
data/raw/
data/processed/
models/

# OS
.DS_Store

# Flutter
flutter/build/
flutter/.dart_tool/
flutter/.flutter-plugins*

# Next.js (frontend/)
frontend/node_modules/
frontend/.next/
frontend/out/

# GCP 서비스 계정 키. 커밋되면 API 키 유출보다 나쁘다 — 이 계정으로 할 수 있는
# 모든 것이 열린다. 파일을 옮기기 전에 이 줄이 먼저 있어야 한다.
gcp-sa.json
*-sa.json
*service-account*.json
