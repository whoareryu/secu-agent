# W4b 시연 · 동작 원리 화면 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 순진한 RAG 가 실제로 새는 것을 화면에서 실행해 보이고, 이 프로젝트가 그 위에서 무엇을 더 했는지를 뒷받침과 함께 말한다.

**Architecture:** `backend/demo/` 가 의도적으로 새는 사후 필터링 경로를 갖는다. 격리는 세 방향 전부 테스트로 강제하며, 특히 `api/` 안에서 그것을 import 하는 파일이 **정확히 하나**인지를 단언한다. 이 경로는 조항 코드와 개수만 돌려주고 본문을 돌려주지 않으며, LLM 을 부르지 않는다. 화면은 라이브인 것과 인용인 것을 스스로 구분해 밝힌다.

**Tech Stack:** Python 3.12 · psycopg 3 · FastAPI · Next.js 16 · pytest

**Spec:** `docs/superpowers/specs/2026-09-01-w4-logs-and-demo-design.md` §5·§6 (상위: `docs/superpowers/specs/2026-08-31-secu-agent-design.md`)

**선행:** W4a Task 1(별도 테스트 DB)·Task 2(권한 SQL 조각). 패널 ④ 는 W4a 전체를 필요로 한다 — Task 7 에서 다룬다.

## Global Constraints

- 커밋 메시지는 한국어, 평서형 과거 (`~했다`).
- `core/` 는 `adapters`·`api`·`pipeline`·`eval`·`demo` 를 import 하지 않는다.
- **`demo/` 를 import 하는 파일은 저장소 전체에서 `api/demo.py` 하나뿐이다.** 테스트가 집합 동일성으로 강제한다.
- 데모 경로는 **본문(chunk text)을 돌려주지 않는다.** 조항 코드와 개수만.
- 데모 경로는 **LLM 을 부르지 않는다.**
- 하드코딩된 색을 프론트엔드 코드에 두지 않는다 — `frontend/app/_ds/industry.css` 의 변수를 쓴다.
- `.blueprint` 를 쓰는 모든 요소는 `frontend/components/Blueprint.tsx` 를 거친다.
- 백엔드 주소와 공유 시크릿은 브라우저에 내려가지 않는다 — `NEXT_PUBLIC_` 금지.
- **화면에 뒷받침 없는 문장을 한 줄도 두지 않는다.** 라이브면 라이브라고, 인용이면 출처를 화면이 밝힌다.

## File Structure

| 파일 | 책임 |
|---|---|
| `backend/demo/__init__.py`, `naive_search.py` (신규) | 의도적으로 새는 사후 필터링 검색 |
| `backend/demo/compare.py` (신규) | 사전·사후 두 경로를 같은 질의로 돌려 개수·조항을 비교 |
| `backend/api/demo.py` (신규) | `POST /demo/compare`. **`demo` 를 import 하는 유일한 파일** |
| `backend/api/main.py` (수정) | 데모 라우터 마운트 |
| `backend/tests/test_demo_isolation.py` (신규) | 격리 세 방향 |
| `backend/tests/test_naive_leaks.py` (신규) | 순진한 경로가 **실제로 새는지** 확인 |
| `frontend/app/api/demo-compare/route.ts` (신규) | BFF |
| `frontend/app/(app)/how/page.tsx` (신규) | 동작 원리 화면 |
| `frontend/components/LeakCompare.tsx` (신규) | 패널 ① 라이브 시연 |
| `frontend/components/EvidencePanel.tsx` (신규) | 패널 ②③④ 공통 껍데기 — 뒷받침 배지를 강제한다 |
| `frontend/lib/types.ts` (수정) | `NAV_ITEMS` 에 동작 원리 |

---

## Task 1: 데모 경로와 격리

격리가 먼저다. 새는 코드를 만들기 전에 그것이 프로덕션에 닿을 수 없다는 것을 테스트로 못 박는다.

**Files:**
- Create: `backend/demo/__init__.py`, `backend/demo/naive_search.py`
- Create: `backend/tests/test_demo_isolation.py`
- Modify: `backend/tests/test_boundaries.py`

**Interfaces:**
- Consumes: `core.ports.ChunkSearch`, `core.types.Principal`
- Produces: `demo.naive_search.naive_ids(conn, vec, principal, k) -> list[int]`

- [ ] **Step 1: 격리 테스트를 먼저 쓴다**

`backend/tests/test_demo_isolation.py`:

```python
"""데모 경로 격리.

demo/ 는 의도적으로 새는 코드를 담는다. 그것이 프로덕션 경로에 닿으면
시연 장치가 곧 취약점이 된다. 세 방향 전부를 막는다.

**핵심은 세 번째 테스트다.** 부분집합이 아니라 집합 동일성을 본다 —
main.py 가 언젠가 편의상 demo 를 끌어다 쓰면 그 순간 터진다.
"""

import ast
from pathlib import Path

import pytest

안쪽_계층 = ["core", "adapters", "pipeline", "eval"]
데모를_아는_파일 = {"api/demo.py"}


def _import_이름들(py: Path) -> set[str]:
    tree = ast.parse(py.read_text(encoding="utf-8"))
    이름 = []
    for node in ast.walk(tree):
        if isinstance(node, ast.Import):
            이름 += [a.name for a in node.names]
        elif isinstance(node, ast.ImportFrom) and node.module:
            이름.append(node.module)
    return {n.split(".")[0] for n in 이름}


def _파일들(*디렉터리: str) -> list[Path]:
    나온다 = []
    for d in 디렉터리:
        나온다 += sorted(Path(d).rglob("*.py"))
    return 나온다


def test_검사할_파일이_있다():
    """0개면 아래 테스트들이 공허하게 통과한다."""
    assert _파일들(*안쪽_계층)
    assert _파일들("api")


@pytest.mark.parametrize("py", _파일들(*안쪽_계층), ids=str)
def test_안쪽_계층은_demo_를_모른다(py):
    assert "demo" not in _import_이름들(py), f"{py} 가 demo 를 import 한다"


def test_api_에서_demo_를_아는_파일은_정확히_하나다():
    """부분집합이 아니라 **동일성**을 본다.

    "api/demo.py 는 demo 를 import 해도 된다" 만 검사하면, main.py 가
    나중에 하나 끌어다 써도 통과한다. 목록 자체를 고정한다.
    """
    실제 = {
        str(py) for py in _파일들("api")
        if "demo" in _import_이름들(py)
    }
    assert 실제 == 데모를_아는_파일, (
        f"demo 를 import 하는 api 파일이 바뀌었다.\n"
        f"기대: {sorted(데모를_아는_파일)}\n실제: {sorted(실제)}\n"
        "새 파일이 정말 demo 를 알아야 한다면 이 테스트의 목록을 함께 고친다 — "
        "그 변경이 리뷰에 보이는 것이 목적이다."
    )


def test_demo_모듈이_자기_성격을_문서화한다():
    """읽는 사람이 이 파일을 프로덕션 코드로 오해하면 안 된다."""
    독스트링 = ast.get_docstring(ast.parse(Path("demo/naive_search.py").read_text("utf-8")))
    assert 독스트링 is not None
    assert "의도적으로" in 독스트링 and "프로덕션" in 독스트링
```

`backend/tests/test_boundaries.py` 의 `바깥_계층` 에 `"demo"` 를 더한다.

- [ ] **Step 2: 실패를 확인한다**

Run: `.venv/bin/python -m pytest tests/test_demo_isolation.py -v`
Expected: FAIL — `demo/naive_search.py` 없음

- [ ] **Step 3: 새는 경로를 쓴다**

`backend/demo/__init__.py` 는 빈 파일.

`backend/demo/naive_search.py`:

```python
"""**이 경로는 의도적으로 샌다. 프로덕션에서 호출되면 안 된다.**

교과서적인 순진한 RAG 다 — 전체 청크에서 상위 k개를 뽑고 **그다음에**
권한으로 거른다. 그러면 결과 개수가 주체의 권한 범위 크기를 그대로
드러내고, 그 개수 차이가 "여기 네가 못 보는 문서가 있다"는 신호가 된다.

이것을 저장소에 두는 이유는 시연이 말이 아니라 실행이어야 하기 때문이다
(보충 spec 결정 17). 프로덕션 경로에 닿지 않는다는 것은
tests/test_demo_isolation.py 가 집합 동일성으로 강제한다.

adapters/db/chunk_search.py 의 by_vector 와 나란히 놓고 읽으면 차이가
한 줄이다 — WHERE 가 ORDER BY 앞에 있는가 뒤에 있는가.
"""

import psycopg

from adapters.db.permission_sql import 권한_WHERE
from core.types import Principal

Vector = list[float]

# 권한 조건이 **바깥**에 있다. 안쪽 서브쿼리는 전체 청크에서 상위 k개를
# 뽑는다 — 그 k개가 전부 권한 밖이면 필터 뒤에 0건이 남는다.
_순진한_SQL = f"""
    SELECT t.id
    FROM (
        SELECT c.id, c.document_id
        FROM chunks c
        ORDER BY c.embedding <=> %(qvec)s
        LIMIT %(k)s
    ) t
    JOIN documents d ON d.id = t.document_id
    WHERE {권한_WHERE("d")}
"""


def naive_ids(conn: psycopg.Connection, vec: Vector, principal: Principal, k: int) -> list[int]:
    """사후 필터링으로 chunk id 를 뽑는다. **새는 경로다.**"""
    with conn.cursor() as cur:
        cur.execute(
            _순진한_SQL,
            {
                "qvec": str(vec),
                "k": k,
                "clearance": principal.clearance,
                "dept": principal.department,
            },
        )
        return [r[0] for r in cur.fetchall()]
```

- [ ] **Step 4: 통과를 확인한다**

Run: `.venv/bin/python -m pytest tests/test_demo_isolation.py tests/test_boundaries.py -v`
Expected: PASS 전부

- [ ] **Step 5: 변이 검사**

`backend/api/main.py` 맨 위에 `from demo import naive_search` 를 넣는다.
Run: `.venv/bin/python -m pytest tests/test_demo_isolation.py -v`
Expected: `test_api_에서_demo_를_아는_파일은_정확히_하나다` 가 실패하며 `실제: ['api/demo.py', 'api/main.py']` 를 보여준다. 되돌린다.

- [ ] **Step 6: 커밋**

```bash
git add backend/demo/ backend/tests/test_demo_isolation.py backend/tests/test_boundaries.py
git commit -m "데모 전용 누출 경로와 그 격리를 더했다"
```

---

## Task 2: 순진한 경로가 실제로 새는지 확인한다

시연의 전제는 "1단계 누출은 현재 코퍼스에서 눈에 보인다" 이다. 그것이 참이 아니면 화면은 아무것도 보여주지 못한다. **먼저 측정한다.**

**Files:**
- Create: `backend/tests/test_naive_leaks.py`

**Interfaces:**
- Consumes: `demo.naive_search.naive_ids`, `adapters.db.chunk_search.PgChunkSearch`, W4a Task 1 의 `db연결` 픽스처

- [ ] **Step 1: 두 경로를 대조하는 테스트를 쓴다**

`backend/tests/test_naive_leaks.py`:

```python
"""순진한 경로가 실제로 새는지 확인한다.

이 테스트가 통과하지 않으면 시연 화면이 성립하지 않는다 — 두 경로가
같은 결과를 내면 보여줄 것이 없다.

test_leakage.py 와 방향이 반대다. 저쪽은 "우리 경로가 새지 않는가" 를
묻고, 이쪽은 "순진한 경로가 정말로 새는가" 를 묻는다. 둘 다 필요하다.
"""

import pytest

from adapters.db.chunk_search import PgChunkSearch
from core.types import Principal
from demo.naive_search import naive_ids

pytestmark = pytest.mark.db

_k = 10
개발자 = Principal(department="개발팀", clearance=1)
임원 = Principal(department="경영지원팀", clearance=3)


@pytest.fixture
def 코퍼스(db연결):
    """공개 문서 + 등급 3 문서. 등급 3 쪽 청크가 질의에 더 가깝게 심는다.

    이 배치가 이 파일의 전부다. 기밀 청크가 질의 근처에 없으면 순진한
    경로도 새지 않고, 그러면 시연이 아무것도 보여주지 못한다.
    """
    # test_leakage.py 의 좌표 블록 방식을 그대로 쓴다 — 난수 단위벡터는
    # 384차원에서 cos 가 0.1 씩 나와 직교가 보장되지 않는다(W2 실측).
    ...  # test_leakage.py 의 _코퍼스 픽스처를 따른다


def test_사전_필터링은_개수가_k_로_고정된다(코퍼스, 임베딩):
    검색 = PgChunkSearch(코퍼스)
    for p in (개발자, 임원):
        assert len(검색.by_vector(임베딩, p, _k)) == _k


def test_순진한_경로는_등급에_따라_개수가_갈린다(코퍼스, 임베딩):
    """**이 테스트가 시연의 전제다.**

    갈리지 않으면 화면에서 보여줄 것이 없다. 그때는 코퍼스 배치를
    고쳐야지 화면 문구를 고치면 안 된다.
    """
    낮음 = len(naive_ids(코퍼스, 임베딩, 개발자, _k))
    높음 = len(naive_ids(코퍼스, 임베딩, 임원, _k))
    assert 높음 == _k
    assert 낮음 < 높음, (
        f"순진한 경로에서도 개수가 같다({낮음} == {높음}). "
        "기밀 청크가 질의 근처에 없다 — 코퍼스 배치를 고친다."
    )


def test_개수_차이가_그대로_존재_신호다(코퍼스, 임베딩):
    """화면이 주장할 문장을 테스트로 고정한다.

    사후 필터링에서 빠진 건수 = 권한 밖 문서가 상위 k에 든 건수.
    """
    낮음 = naive_ids(코퍼스, 임베딩, 개발자, _k)
    assert _k - len(낮음) > 0
```

- [ ] **Step 2: 픽스처를 실제로 채운다**

`tests/test_leakage.py` 의 `_코퍼스` 픽스처와 좌표 블록 임베딩 생성을 읽고 같은 방식으로 채운다. **난수 단위벡터를 쓰지 않는다** — W2 실측에서 384차원 난수 두 개의 cos 가 0.101 이 나왔고, "기밀 축 근처" 로 심은 청크가 무관해야 할 질의의 상위 10건 중 9건을 차지했다.

다만 규모는 다르다. `test_leakage.py` 는 문서당 2,000청크가 필요했는데 그것은 `AS MATERIALIZED` 변이(2단계 누출)를 잡기 위해서였다. 여기서 잡는 것은 **1단계** — 사후 필터링 — 이라 훨씬 작은 규모에서 보인다. 문서당 50청크로 시작해 위 테스트가 통과하는 최소 규모를 찾고, 그 숫자와 근거를 파일 상단 주석에 적는다.

- [ ] **Step 3: 통과를 확인한다**

Run: `.venv/bin/python -m pytest -m db tests/test_naive_leaks.py -v`
Expected: PASS 3건

- [ ] **Step 4: 실제 코퍼스에서도 보이는지 손으로 확인한다**

시연 화면은 픽스처가 아니라 **작업 코퍼스**(9문서·338청크) 위에서 돈다. 그 위에서도 갈리는지 확인한다:

```bash
cd backend && .venv/bin/python - <<'PY'
from adapters.db.connection import connect
from adapters.embedding.e5 import E5Embedder
from adapters.db.chunk_search import PgChunkSearch
from demo.naive_search import naive_ids
from core.types import Principal

conn = connect()
vec = E5Embedder().encode(["임원 성과급은 어떤 기준으로 정해지나"], kind="query")[0]
검색 = PgChunkSearch(conn)
for 이름, p in [("김개발", Principal("개발팀", 1)),
                ("박인사", Principal("인사팀", 2)),
                ("최임원", Principal("경영지원팀", 3))]:
    print(이름, "사전", len(검색.by_vector(vec, p, 10)), "/ 사후", len(naive_ids(conn, vec, p, 10)))
PY
```

Expected: 사전은 셋 다 10, 사후는 등급에 따라 갈린다.

**갈리지 않으면 Task 5 로 넘어가기 전에 멈추고 보고한다.** 질의를 바꿔봐도 갈리지 않는다면 시연의 전제가 이 코퍼스에서 성립하지 않는 것이고, 그건 화면 문구로 덮을 문제가 아니다.

- [ ] **Step 5: 커밋**

```bash
git add backend/tests/test_naive_leaks.py
git commit -m "순진한 경로가 실제로 새는 것을 테스트로 못 박았다"
```

---

## Task 3: 비교 API

**Files:**
- Create: `backend/demo/compare.py`, `backend/api/demo.py`
- Create: `backend/tests/test_demo_api.py`
- Modify: `backend/api/main.py`, `backend/api/schemas.py`

**Interfaces:**
- Produces: `POST /demo/compare` — 요청 `{query: str, k: int}`, 응답 `{query, k, personas: [{name, department, clearance, prefiltered: {count, clause_codes}, naive: {count, clause_codes}}]}`

- [ ] **Step 1: 응답 계약을 테스트로 쓴다**

`backend/tests/test_demo_api.py`:

```python
"""데모 비교 API.

**본문을 돌려주지 않는다.** 누출을 보여주는 데 본문이 필요 없다 —
개수와 조항 코드면 개수 채널과 순위 채널이 둘 다 보인다. 시연을 위해서라도
본문이 이 경로로 나가지 않는다는 불변식은 깨지 않는다(보충 spec 결정 18).
"""

import pytest


def test_응답에_본문이_없다(demo_client):
    r = demo_client.post("/demo/compare", json={"query": "비밀번호", "k": 10},
                         headers=_시크릿)
    본문 = r.text
    assert "text" not in r.json()["personas"][0]["prefiltered"]
    # 실제 청크 본문의 한 조각이 응답 어디에도 없어야 한다
    assert "제1조" not in 본문


def test_세_페르소나가_모두_나온다(demo_client):
    r = demo_client.post("/demo/compare", json={"query": "비밀번호", "k": 10},
                         headers=_시크릿)
    이름 = [p["name"] for p in r.json()["personas"]]
    assert 이름 == ["김개발", "박인사", "최임원"]


def test_사전_필터링_개수가_k_로_고정된다(demo_client):
    r = demo_client.post("/demo/compare", json={"query": "비밀번호", "k": 10},
                         headers=_시크릿)
    assert all(p["prefiltered"]["count"] == 10 for p in r.json()["personas"])


def test_k_에_상한이_있다(demo_client):
    """모델이 부르는 경로는 아니지만 브라우저가 부른다 — 값을 믿지 않는다."""
    r = demo_client.post("/demo/compare", json={"query": "비밀번호", "k": 9999},
                         headers=_시크릿)
    assert r.json()["k"] <= 20
    assert all(p["prefiltered"]["count"] <= 20 for p in r.json()["personas"])


def test_LLM_을_부르지_않는다(demo_client_모델_없음):
    """모델 자격증명이 아예 없어도 200 이어야 한다.

    이 경로가 무료라는 주장이 참인지를 구조로 확인한다.
    """
    r = demo_client_모델_없음.post("/demo/compare", json={"query": "비밀번호", "k": 10},
                                  headers=_시크릿)
    assert r.status_code == 200


def test_시크릿_없이는_401(demo_client):
    r = demo_client.post("/demo/compare", json={"query": "비밀번호", "k": 10})
    assert r.status_code == 401
```

- [ ] **Step 2: 실패를 확인하고 구현한다**

`backend/demo/compare.py`:

```python
"""사전·사후 두 경로를 같은 질의로 돌려 비교한다.

임베딩은 한 번만 계산해 여섯 번의 검색이 공유한다. LLM 호출이 없어
이 경로는 요금이 0원이다 — 시연을 몇 번 돌려도 마찬가지다.
"""

from dataclasses import dataclass

import psycopg

from adapters.db.chunk_search import PgChunkSearch
from core.ports import Embedder
from core.types import Principal
from demo.naive_search import naive_ids

MAX_DEMO_K = 20


@dataclass(frozen=True)
class 경로결과:
    count: int
    clause_codes: list[str]


def _조항코드(conn: psycopg.Connection, ids: list[int]) -> list[str]:
    """chunk id → 조항 코드. **본문은 읽지 않는다.**

    순서는 준 id 순서를 따른다 — 순위 채널을 보여주려면 순서가 의미를 갖는다.
    """
    if not ids:
        return []
    with conn.cursor() as cur:
        cur.execute(
            "SELECT c.id, cl.code FROM chunks c"
            " LEFT JOIN clauses cl ON cl.id = c.clause_id"
            " WHERE c.id = ANY(%s)",
            (ids,),
        )
        코드 = dict(cur.fetchall())
    return [코드.get(i) or "조항 밖" for i in ids]


def compare(conn, embedder: Embedder, query: str, k: int, principals: list[tuple[str, Principal]]):
    k = max(1, min(k, MAX_DEMO_K))
    vec = embedder.encode([query], kind="query")[0]
    검색 = PgChunkSearch(conn)

    나온다 = []
    for 이름, p in principals:
        사전 = 검색.by_vector(vec, p, k)
        사후 = naive_ids(conn, vec, p, k)
        나온다.append({
            "name": 이름,
            "department": p.department,
            "clearance": p.clearance,
            "prefiltered": 경로결과(len(사전), _조항코드(conn, 사전)),
            "naive": 경로결과(len(사후), _조항코드(conn, 사후)),
        })
    return {"query": query, "k": k, "personas": 나온다}
```

`backend/api/demo.py` — **`demo` 를 import 하는 유일한 파일이다.** 시크릿 검사는 `api/security.py` 의 것을 그대로 쓰고, 라우터를 만들어 `main.py` 가 `include_router` 로 마운트한다. 세션·관리자 검사는 BFF 쪽에서 한다.

- [ ] **Step 3: 통과를 확인한다**

Run: `.venv/bin/python -m pytest -m db tests/test_demo_api.py -v`
Expected: PASS 6건

- [ ] **Step 4: 격리 테스트가 여전히 통과하는지 본다**

Run: `.venv/bin/python -m pytest tests/test_demo_isolation.py -v`
Expected: PASS — `api/demo.py` 하나만 `demo` 를 안다

- [ ] **Step 5: 컨테이너를 다시 빌드하고 실물로 확인한다**

```bash
docker compose up -d --build backend
curl -s -X POST localhost:18080/demo/compare -H "X-Backend-Secret: local-dev-secret" \
  -H 'Content-Type: application/json' \
  -d '{"query":"임원 성과급은 어떤 기준으로 정해지나","k":10}' | python3 -m json.tool
```

Expected: `prefiltered.count` 가 셋 다 10, `naive.count` 는 갈린다.

- [ ] **Step 6: 커밋**

```bash
git add backend/demo/compare.py backend/api/ backend/tests/test_demo_api.py
git commit -m "사전·사후 비교 API 를 더했다"
```

---

## Task 4: BFF 와 화면 뼈대

**Files:**
- Create: `frontend/app/api/demo-compare/route.ts`, `frontend/app/(app)/how/page.tsx`, `frontend/components/EvidencePanel.tsx`
- Modify: `frontend/lib/types.ts`

**Interfaces:**
- Produces: `EvidencePanel` — `{kicker, title, evidence: "live" | "measured" | "schema", source?: string, children}`

- [ ] **Step 1: BFF 라우트를 쓴다**

`frontend/app/api/demo-compare/route.ts` — `frontend/app/api/principals/route.ts` 의 형태를 그대로 따른다. 세션이 있으면 통과(관리자 전용이 아니다 — 이 화면이 곧 설명이다), `POST` 본문을 그대로 백엔드에 넘긴다.

- [ ] **Step 2: 뒷받침 배지를 강제하는 껍데기를 만든다**

`frontend/components/EvidencePanel.tsx`:

```tsx
// 이 화면의 모든 패널은 자기 주장의 뒷받침을 함께 말한다.
//
// evidence 를 선택값이 아니라 필수 prop 으로 둔 이유: 뒷받침 없는 패널을
// 만들 수 없게 하기 위해서다. 이 프로젝트는 "저장하는 곳이 없는 값을
// 화면에 두지 않는다"를 지켜왔고, 주장만 적힌 설명 화면은 그 규칙을
// 스스로 깨는 것이다.
type Evidence = "live" | "measured" | "schema";

const 배지: Record<Evidence, { 글: string; 설명: string }> = {
  live: { 글: "지금 실행됨", 설명: "이 숫자는 이 페이지를 열 때 실제로 계산됐습니다." },
  measured: { 글: "측정 기록", 설명: "라이브가 아닙니다. 아래 출처에서 측정된 값입니다." },
  schema: { 글: "스키마", 설명: "코드와 스키마에서 그대로 읽은 것입니다." },
};
```

`source` 는 `evidence === "measured"` 일 때 **필수**로 만든다 — 타입 수준에서 분기 유니온으로 강제한다:

```tsx
type Props =
  | { evidence: "live" | "schema"; source?: never; kicker: string; title: string; children: React.ReactNode }
  | { evidence: "measured"; source: string; kicker: string; title: string; children: React.ReactNode };
```

이렇게 하면 출처 없는 인용 패널이 **컴파일되지 않는다.**

- [ ] **Step 3: 화면과 네비게이션을 붙인다**

`frontend/lib/types.ts` 의 `NAV_ITEMS` 에 더한다 (질의 바로 다음, 문서 앞):

```ts
{ id: "how", ko: "동작 원리", en: "How it works", badge: "", href: "/how", adminOnly: false },
```

`frontend/app/(app)/how/page.tsx` 는 네 패널의 자리만 잡고 각각 "다음 태스크에서" 대신 **실제 내용의 첫 판**을 넣는다 — 빈 자리를 남기지 않는다.

- [ ] **Step 4: 게이트**

```bash
cd frontend && rm -f tsconfig.tsbuildinfo && npx tsc --noEmit && npm test && npm run build
```

- [ ] **Step 5: 커밋**

```bash
git add frontend/app/api/demo-compare frontend/app/\(app\)/how frontend/components/EvidencePanel.tsx frontend/lib/types.ts
git commit -m "동작 원리 화면의 뼈대와 뒷받침 배지를 더했다"
```

---

## Task 5: 패널 ① — 라이브 누출 시연

**Files:**
- Create: `frontend/components/LeakCompare.tsx`
- Modify: `frontend/app/(app)/how/page.tsx`

- [ ] **Step 1: 컴포넌트를 만든다**

`LeakCompare.tsx` 가 하는 일:

1. 질의 입력. 기본값은 갈라짐이 잘 보이는 것으로 — Task 2 Step 4 에서 확인한 질의를 쓴다.
2. `POST /api/demo-compare` 를 부른다.
3. **사전 필터링** 열: 세 페르소나의 `count` 와 조항 코드 목록.
4. 토글 **"순진한 방식으로 다시"** → **사후 필터링** 열로 바꾼다. 개수가 갈라지는 것이 보인다.
5. 갈라진 개수 옆에 그 의미를 적는다 — `10 → 6` 이면 *"4건이 사라졌습니다. 그 4가 곧 '여기 네가 못 보는 문서가 있다'는 신호입니다."*

**주장은 조건 없이 참인 것만 쓴다.** 최종 리뷰가 `Answer.tsx` 에서 지적한 것과 같은 함정이다:

```tsx
// 이렇게 쓰지 않는다 — 허용 문서가 k 미만이면 거짓이 된다.
// "세 계정 모두 정확히 같은 개수를 받습니다"
//
// 이렇게 쓴다:
// "사전 필터링에서는 개수가 권한 범위의 크기가 아니라 k 에 의해 정해집니다."
// "사후 필터링에서는 개수가 권한 범위의 크기를 그대로 드러냅니다."
```

- [ ] **Step 2: 로딩·실패 상태를 만든다**

임베딩 계산이 있어 첫 호출이 느릴 수 있다. 로딩 문구는 사실만 적는다 — *"세 계정 × 두 경로, 여섯 번 검색합니다. 모델은 부르지 않습니다."* 실패하면 지어낸 값을 보여주지 말고 실패를 적는다.

- [ ] **Step 3: 브라우저에서 확인한다**

`npm run dev` 로 `/how` 를 열고:
- 사전 필터링에서 세 계정의 개수가 같다
- 토글하면 개수가 갈라진다
- 갈라진 숫자와 설명 문구가 맞다
- `.blueprint` 요소에 코너 마크 4개가 있다

- [ ] **Step 4: 게이트와 커밋**

```bash
cd frontend && rm -f tsconfig.tsbuildinfo && npx tsc --noEmit && npm test && npm run build
git add frontend/components/LeakCompare.tsx frontend/app/\(app\)/how
git commit -m "누출 시연 패널을 라이브로 붙였다"
```

---

## Task 6: 패널 ②③ — 인용과 스키마

**Files:**
- Modify: `frontend/app/(app)/how/page.tsx`

- [ ] **Step 1: 패널 ② — 2단계 누출 (evidence="measured")**

내용: 사전 필터링을 넣어도 플래너가 CTE 를 인라인하면 HNSW 근사 검색이 후보를 잘라 다시 샌다.

**숫자는 `backend/tests/test_leakage.py` 의 상수와 주석에서 그대로 옮긴다.** 문서당 300개(총 600)면 여전히 10건이 나오고, 2,000개(총 4,000)여야 0건이 된다.

`source` 에 파일 경로와 그 상수 이름을 적는다. 화면이 **"라이브가 아니다"** 를 스스로 말한다 — 왜 라이브가 아닌지도: 현재 코퍼스 338청크는 이 현상이 나타나는 규모가 아니다.

이 패널이 이 화면에서 가장 중요하다. NAS 유비에 대한 답이 여기 있다:

> 1단계는 "이렇게 짜면 샌다" 를 보여준다. 2단계는 **"고친 줄 알았는데 여전히 샜다"** 를 보여준다. 두 번째는 테스트 없이 알 수 없다.

- [ ] **Step 2: 패널 ③ — 기록이 담지 않는 것 (evidence="schema")**

`access_records` 의 컬럼을 그대로 보여준다. 본문도 제목도 없다. 제목만으로 존재가 드러나기 때문이며, `AccessViolation` 이 메시지에 `chunk_id` 만 담는 것과 같은 원칙이다.

컬럼을 더하면 실패하는 테스트가 둘 있다는 것까지 적고, **그 테스트 이름을 적는다.**

- [ ] **Step 3: 브라우저 확인과 커밋**

`measured` 패널에 `source` 가 없으면 컴파일이 안 되는 것을 이용해, 출처를 빠뜨리지 않았는지 타입 검사가 보증한다.

```bash
cd frontend && rm -f tsconfig.tsbuildinfo && npx tsc --noEmit && npm run build
git commit -am "2단계 누출과 기록 스키마 패널을 더했다"
```

---

## Task 7: 패널 ④ — 같은 불변식, 두 데이터 타입

**W4a 가 끝난 뒤에 한다.** 로그 쪽이 없으면 이 패널은 뒷받침이 없다.

**Files:**
- Modify: `frontend/app/(app)/how/page.tsx`
- Modify: `README.md`, `jekyll/verification.markdown`

- [ ] **Step 1: 패널 ④ 를 만든다 (evidence="live" 또는 "schema")**

보충 스펙 §2.3 의 표를 화면에 옮긴다:

| | 문서 | 로그 |
|---|---|---|
| 관측되는 값 | 결과 k개 (고정) | 이벤트 개수 (가변) |
| 위험 | 개수 차이로 존재 노출 | 부분 집계를 전체로 오해 |
| 장치 | 사전 필터링 + 고정 k | 사전 필터링 + 무조건적 범위 고지 |

**그리고 그 아래에 실제 증거를 놓는다** — `core/access/visibility.py` 의 `visible()` 하나가 문서와 로그 양쪽을 판정한다는 것, 그리고 권한 SQL 조각이 저장소에 하나뿐이라는 것. `grep` 한 줄로 확인 가능하다는 사실까지 적는다.

- [ ] **Step 2: 검증 기록에 항목을 더한다**

`jekyll/verification.markdown` 에 항목 ⑧:

```markdown
#### 장치가 잡은 것 ⑧: "이건 NAS 로 되잖아" 가 절반 맞았다

현업에서 보안을 NAS 로 운영하던 사람에게 이 피드백을 받았습니다.
**절반은 맞습니다.** 시놀로지에 정책 문서를 올리고 폴더 ACL 을 걸면 권한
없는 사용자는 검색 결과에서 그 파일을 못 보고 존재조차 모릅니다.
"권한 없는 것은 존재조차 알 수 없다" 는 파일시스템 ACL 에서 공짜로
따라옵니다.

**유비가 깨지는 지점은 파생 인덱스입니다.** NAS 는 파일이 저장 단위이고
ACL 이 그 위에 붙습니다. RAG 는 모든 등급의 청크가 하나의 벡터 테이블에
섞이고, 그 인덱스에는 ACL 을 붙일 자리가 없습니다.

문제는 그 사실이 테스트 파일 안에만 있었다는 것입니다. 화면을 보는
사람은 "문서 검색되네" 까지만 봤습니다. 그래서 순진한 경로를 실제로
만들어 나란히 실행합니다 — 기능을 더한 것이 아니라 이미 한 일을
드러낸 것입니다.
```

- [ ] **Step 3: README 에 한 문단**

`/how` 화면이 무엇을 보여주는지, 그리고 데모 전용 누출 경로가 저장소에 있으며 프로덕션 경로에 닿지 않는다는 것이 테스트로 강제된다는 사실.

- [ ] **Step 4: 전체 게이트와 커밋**

```bash
cd backend && .venv/bin/python -m pytest -q -m "not db" && .venv/bin/python -m pytest -q -m db && .venv/bin/python -m ruff check .
cd ../frontend && rm -f tsconfig.tsbuildinfo && npm test && npx tsc --noEmit && npm run build
git commit -am "두 데이터 타입 패널과 검증 기록을 더했다"
```

---

## 완료 조건

- [ ] `pytest -q -m "not db"` · `pytest -q -m db` · `ruff check` 통과
- [ ] `npm test` · `npx tsc --noEmit` · `npm run build` 통과
- [ ] **`demo` 를 import 하는 파일이 `api/demo.py` 하나뿐이고, 그것이 테스트로 강제된다**
- [ ] `api/main.py` 에 `demo` import 를 넣으면 격리 테스트가 실패한다 (확인 후 되돌림)
- [ ] 데모 응답 어디에도 청크 본문이 없다
- [ ] 모델 자격증명이 없어도 `/demo/compare` 가 200 이다
- [ ] `/how` 에서 토글하면 개수가 실제로 갈라진다
- [ ] **모든 패널이 뒷받침 배지를 갖는다** — 출처 없는 `measured` 패널은 컴파일되지 않는다
- [ ] 화면의 주장이 전부 조건 없이 참이다 — "정확히 같은 개수" 같은 무조건문이 없다
- [ ] `.blueprint` 를 쓴 모든 곳에 코너 마크 4개가 있다
- [ ] 하드코딩된 색이 없다

## 다음 계획으로 넘길 것

- **`draft_report` · `verify_clauses`** — 상위 spec §7.1·7.3
- **2단계 누출의 라이브 시연** — 현재 코퍼스 규모로는 재현되지 않는다. 대규모 합성 코퍼스를 별도 데이터베이스에 두면 가능하다
- **반응형** — 핸드오프가 데스크톱 전용(1240px)이라고 명시했다
